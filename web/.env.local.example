# Base URL of the FastAPI backend (backend/). No trailing slash.
# Local dev default is http://127.0.0.1:5000 when this is unset.
# In production set this to your deployed FastAPI URL, e.g.
#   NEXT_PUBLIC_API_BASE=https://teacher-panel-d1kw.onrender.com
NEXT_PUBLIC_API_BASE=http://127.0.0.1:5000
